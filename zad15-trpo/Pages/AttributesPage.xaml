<Page x:Class="zad15_trpo.Pages.AttributesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:zad15_trpo.Pages"
      mc:Ignorable="d" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AttributesPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <!-- Бренды -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <DataGrid x:Name="dgBrands" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionChanged="dgBrands_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,10,0,0" VerticalAlignment="Center">
                <TextBox x:Name="txtBrandName" Width="200" VerticalAlignment="Center" materialDesign:HintAssist.Hint="Введите Новый бренд"/>
                <Button x:Name="btnBrandAdd" Content="Добавить" Click="btnBrandAdd_Click" Margin="5,0,0,0"/>
                <Button x:Name="btnBrandUpdate" Content="Изменить" Click="btnBrandUpdate_Click" Margin="5,0,0,0"/>
                <Button Content="Удалить выбранное" Click="btnBrandDelete_Click" Margin="5,0,0,0"/>
                <Button Content="Назад" Click="Button_Click"></Button>
            </StackPanel>
        </Grid>

        <!-- Категории -->

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <DataGrid x:Name="dgCategories" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionChanged="dgCategories_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,10,0,0">
                <TextBox x:Name="txtCategoryName" Width="200" VerticalAlignment="Center" materialDesign:HintAssist.Hint="Введите Новую категорию"/>
                <Button x:Name="btnCatAdd" Content="Добавить" Click="btnCatAdd_Click" Margin="5,0,0,0"/>
                <Button x:Name="btnCatUpdate" Content="Изменить" Click="btnCatUpdate_Click" Margin="5,0,0,0"/>
                <Button  Content="Удалить выбранное" Click="btnCatDelete_Click" Margin="5,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Теги -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <DataGrid x:Name="dgTags" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionChanged="dgTags_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Grid.Row="1" Orientation="Vertical">
                <TextBox x:Name="txtTagName" Width="200" VerticalAlignment="Center" materialDesign:HintAssist.Hint="Введите Новый тег"/>
                <Button x:Name="btnTagAdd" Content="Добавить" Click="btnTagAdd_Click" Margin="5,0,0,0"/>
                <Button x:Name="btnTagUpdate" Content="Изменить" Click="btnTagUpdate_Click" Margin="5,0,0,0"/>
                <Button Content="Удалить выбранное" Click="btnTagDelete_Click" Margin="5,0,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
