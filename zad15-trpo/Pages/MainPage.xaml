<Page x:Class="zad15_trpo.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:zad15_trpo.Pages"
      mc:Ignorable="d" 
      DataContext="{Binding RelativeSource={RelativeSource Self}}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <Label>Поиск</Label>
            <TextBox Text="{Binding searchQuery, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Введите текст для поиска" TextChanged="TextBox_TextChanged"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="0">
            <Label FontSize="16">Сортировка</Label>
            <ComboBox>
                <ComboBox.Items>
                    <ComboBoxItem Tag="Name">Наименование</ComboBoxItem>
                    <ComboBoxItem Tag="PriceMin">Цена (убывание)</ComboBoxItem>
                    <ComboBoxItem Tag="PriceMax">Цена (возрастание)</ComboBoxItem>
                    <ComboBoxItem Tag="CountMin">Количество (убывание)</ComboBoxItem>
                    <ComboBoxItem Tag="CountMax">Количество (возрастание)</ComboBoxItem>
                </ComboBox.Items>
            </ComboBox>
            <Label>Фильтры</Label>
            <StackPanel>
                <ComboBox>
                    <ComboBox.Items>
                        <ComboBoxItem Tag="Category">По категории</ComboBoxItem>
                        <ComboBoxItem Tag="Brand">По бренду</ComboBoxItem>
                        <ComboBoxItem Tag="Price">Цена</ComboBoxItem>
                    </ComboBox.Items>
                </ComboBox>
                <Label>от</Label>
                <TextBox />
                <Label>до</Label>
                <TextBox />
            </StackPanel>
            <Button Margin="5" Click="Button_Click">Сбросить</Button>
        </StackPanel>
        <materialDesign:ColorZone Grid.Row="1" Grid.Column="1" Mode="SecondaryMid" >
            <ListView Grid.Row="1"
                  Grid.Column="1"
                  ItemsSource="{Binding service.Products}"
                  HorizontalContentAlignment="Stretch"
                  Margin="10"
                  Name="ProductsList">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="{Binding Name}"/>
                            <TextBlock Grid.Row="0" HorizontalAlignment="Right">
                            <Run Text="Цена: "/>
                            <Run Text="{Binding Price}"/>
                            </TextBlock>

                            <TextBlock Grid.Row="1">
                            <Run Text="⭐ "/>
                            <Run Text="{Binding Rating}"/>
                            </TextBlock>
                            <TextBlock Grid.Row="2">
                            <Run Text="Категория: "/>
                            <Run Text="{Binding Category.Name}"/>
                            </TextBlock>
                            <TextBlock Grid.Row="3" Text="{Binding Description}"/>
                            <TextBlock Grid.Row="3" HorizontalAlignment="Right">
                            <Run Text="Остаток: "/>
                            <Run Text="{Binding Stock}"/>
                            </TextBlock>
                            <ItemsControl Grid.Row="4" ItemsSource="{Binding Tags}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock>
                                        <Run Text=" #"/>
                                        <Run Text="{Binding Name}"/>
                                        </TextBlock>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </materialDesign:ColorZone>
        <WrapPanel Grid.Row="2" Grid.ColumnSpan="2">
            <TextBlock/>
            <TextBlock/>
        </WrapPanel>
    </Grid>
</Page>
